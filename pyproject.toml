[tool.poetry]
name = "qa-assignment-webpage"
version = "0.2"
description = ""
authors = ["Reilly Hewitson"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
django = "^4"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
